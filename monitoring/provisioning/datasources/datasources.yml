apiVersion: 1

datasources:
  - name: test-frser-sqlite-datasource
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      path: /var/lib/grafana/data/tweet.db/tweet.db
      readOnly: true